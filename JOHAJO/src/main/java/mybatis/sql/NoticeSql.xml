<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="notice">
	<select id="noticeTotalCount" resultType="int">
		select count(*) from question2 where selection=1
	</select>
	<select id="noticePagingList" resultType="ndto" parameterType="HashMap">
		<![CDATA[select a.* from (select ROWNUM as RNUM,b.* from (select * from question2 where selection=1 order by idx desc)b )a where a.RNUM>=#{start} and a.RNUM<=#{end}]]>
	</select>
	<select id="noticeSelectData" parameterType="int" resultType="ndto">
		select * from question2 where idx=#{idx}
	</select>
</mapper>